/// Maximum length of a building name
pub const MAX_BUILDING_NAME_LEN: usize = 32;

/// Maximum length of a building address
pub const MAX_BUILDING_ADDRESS_LEN: usize = 64;

/// Denominator of BPS (Basis Points)
pub const BPS_DENOMINATOR: u64 = 10_000;

/// Denominator of geo coordinates (Basis Points)
pub const COORD_DENOMINATOR: u64 = 10000000000;

/// Maximum length of a device model
pub const MAX_DEVICE_MODEL_LEN: usize = 32;

/// Maximum length of a device manufacturer
pub const MAX_DEVICE_MANUFACTURER_LEN: usize = 64;
